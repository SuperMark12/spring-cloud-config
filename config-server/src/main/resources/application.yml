server:
  port: 8880

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SuperMark12/spring-cloud-config.git
          search-paths:
          username:
          password:
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8080/eureka/


